type Options = {
    autosizeFont?: boolean,
    debug?: boolean,
    encoding?: string,
    chapters?: Array<number>,
    includePageNumbers?: boolean,
    maxHeight?: number,
    maxWidth?: number,
    numPreviewSlides?: number,
    previewMode?: boolean,
    quiet?: boolean,
    stylesheet?: string,
    vttSettings: string,
    verbose?: boolean
};

export { Options };